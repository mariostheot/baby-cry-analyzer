# Baby Cry AI - training pipeline dependencies.
# Tested with Python 3.10/3.11 (Google Colab). Pin ranges kept broad on purpose so
# the notebook keeps working as Colab updates its base image.

tensorflow>=2.15,<2.17
tensorflow-hub>=0.16
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
librosa>=0.10
soundfile>=0.12
resampy>=0.4
pandas>=2.0
matplotlib>=3.7
pyyaml>=6.0
tqdm>=4.66

# Optional dataset access (only needed if you enable those sources):
#   kaggle    -> Kaggle datasets (needs ~/.kaggle/kaggle.json)
#   huggingface-hub -> CryCeleb2023 (needs `huggingface-cli login`)
kaggle>=1.6
huggingface-hub>=0.23
